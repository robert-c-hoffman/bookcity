#!/bin/bash
# Deploy script for Shelfarr
# Usage: ./script/deploy

set -e

VERSION=$(cat VERSION 2>/dev/null || echo "unknown")

echo "Deploying Shelfarr (v${VERSION})"

# Build and restart
docker-compose up -d --build

echo "Deployment complete!"
