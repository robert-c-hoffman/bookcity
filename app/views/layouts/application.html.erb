<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Shelfarr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Shelfarr">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center gap-6">
            <%= link_to "Shelfarr", root_path, class: "text-xl font-bold text-gray-900" %>
            <% if authenticated? %>
              <%= link_to "Search", search_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
              <%= link_to "Requests", requests_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
              <%= link_to "Library", library_index_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
            <% end %>
          </div>
          <div class="flex items-center gap-4">
            <% if authenticated? %>
              <% if Current.user&.admin? %>
                <%= link_to "Admin", admin_root_path, class: "text-gray-600 hover:text-gray-900" %>
              <% end %>
              <% unread_count = Current.user&.notifications&.unread&.count || 0 %>
              <%= link_to notifications_path, class: "relative text-gray-600 hover:text-gray-900", title: "Notifications" do %>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9" />
                </svg>
                <% if unread_count > 0 %>
                  <span class="absolute -top-1 -right-1 bg-red-500 text-white text-xs font-bold w-5 h-5 flex items-center justify-center rounded-full">
                    <%= unread_count > 9 ? '9+' : unread_count %>
                  </span>
                <% end %>
              <% end %>
              <%= link_to profile_path, class: "text-gray-600 hover:text-gray-900 flex items-center gap-1" do %>
                <span><%= Current.user&.name %></span>
                <% if Current.user&.admin? %>
                  <span class="text-xs bg-purple-100 text-purple-800 px-2 py-1 rounded">Admin</span>
                <% end %>
              <% end %>
              <%= button_to "Sign out", session_path, method: :delete, class: "text-gray-600 hover:text-gray-900" %>
            <% else %>
              <%= link_to "Sign in", new_session_path, class: "text-gray-600 hover:text-gray-900" %>
              <% if User.none? %>
                <%= link_to "Sign up", sign_up_path, class: "rounded-md px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white" %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto mt-8 px-4 sm:px-6 lg:px-8">
      <% if flash[:notice].present? %>
        <div class="mb-6 bg-green-50 border border-green-200 rounded-lg p-4 text-green-700">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert].present? %>
        <div class="mb-6 bg-red-50 border border-red-200 rounded-lg p-4 text-red-700">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
