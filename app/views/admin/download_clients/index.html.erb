<div class="mx-auto max-w-5xl">
  <div class="flex justify-between items-center mb-8">
    <h1 class="font-bold text-4xl">Download Clients</h1>
    <%= link_to "New Client", new_admin_download_client_path, class: "rounded-md px-4 py-2 bg-blue-600 hover:bg-blue-500 text-white" %>
  </div>

  <% if notice = flash[:notice] %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block"><%= notice %></p>
  <% end %>

  <% if alert = flash[:alert] %>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block"><%= alert %></p>
  <% end %>

  <% if @download_clients.empty? %>
    <div class="bg-gray-50 rounded-lg p-8 text-center">
      <p class="text-gray-500 mb-4">No download clients configured yet.</p>
      <%= link_to "Add your first client", new_admin_download_client_path, class: "text-blue-600 hover:text-blue-900" %>
    </div>
  <% else %>
    <% %w[torrent usenet].each do |type| %>
      <% clients = @download_clients.select { |c| type == 'torrent' ? c.qbittorrent? : c.sabnzbd? } %>
      <% next if clients.empty? %>

      <div class="mb-8">
        <h2 class="text-xl font-semibold text-gray-800 mb-4"><%= type.titleize %> Clients</h2>
        <div class="bg-white rounded-lg shadow overflow-hidden">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Priority</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">URL</th>
                <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% clients.each_with_index do |client, index| %>
                <tr>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="flex items-center gap-1">
                      <span class="text-gray-600 font-medium"><%= client.priority + 1 %></span>
                      <div class="flex flex-col ml-2">
                        <% if index > 0 %>
                          <%= button_to move_up_admin_download_client_path(client), method: :post, class: "text-gray-400 hover:text-gray-600" do %>
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7"></path></svg>
                          <% end %>
                        <% end %>
                        <% if index < clients.length - 1 %>
                          <%= button_to move_down_admin_download_client_path(client), method: :post, class: "text-gray-400 hover:text-gray-600" do %>
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg>
                          <% end %>
                        <% end %>
                      </div>
                    </div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <div class="font-medium text-gray-900"><%= client.name %></div>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded <%= client.qbittorrent? ? 'bg-orange-100 text-orange-800' : 'bg-blue-100 text-blue-800' %>">
                      <%= client.client_type.titleize %>
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-gray-500 text-sm"><%= client.url %></td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <span class="px-2 py-1 text-xs rounded <%= client.enabled? ? 'bg-green-100 text-green-800' : 'bg-gray-100 text-gray-800' %>">
                      <%= client.enabled? ? 'Enabled' : 'Disabled' %>
                    </span>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                    <%= button_to "Test", test_admin_download_client_path(client), method: :post, class: "text-green-600 hover:text-green-900 mr-3" %>
                    <%= link_to "Edit", edit_admin_download_client_path(client), class: "text-blue-600 hover:text-blue-900 mr-3" %>
                    <%= button_to "Delete", admin_download_client_path(client), method: :delete, class: "text-red-600 hover:text-red-900", data: { turbo_confirm: "Are you sure you want to delete this client?" } %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
